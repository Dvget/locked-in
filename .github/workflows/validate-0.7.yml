name: Validate LOCKED IN 0.7
on:
  push:
    branches: [ codex/locked-in-0.7 ]
  pull_request:
    branches: [ main ]
permissions:
  contents: read
jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - run: swift test
